{% extends "base.html.twig" %}

{% block body %}
	<header>
		<nav>

			<div>
				<img src="{# {{asset()}} #}" alt="le logo de Knowledge-Learning" class="nav-logo"></img>
		</div>

		<div>
			<ul class="nav-list">
				<li>
					<a href="{{path('app_home')}}">Accueil</a>
				</li>
				<li>
					<a href="{{path('app_register')}}">S'inscrire</a>
				</li>
				<li>
					<a href="{{path('app_theme')}}">Cours</a>
				</li>
				<li>
					<a href="{{path('app_login')}}">Se connecter</a>
				</li>
			</ul>
		</div>

	</nav>
</header>{% endblock %}
